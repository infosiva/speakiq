/**
 * site.config.ts — root-level re-export
 * The canonical config lives at src/lib/site.config.ts
 * This file is a convenience re-export for scripts / tools that run from the project root.
 */
export { siteConfig } from './src/lib/site.config'
